{
  "name": "repohub-category-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker that looks up App Store categories by Bundle ID and caches results in KV",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
